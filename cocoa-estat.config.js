/**
 * cocoa-estat の設定ファイル
 * 
 * このファイルはNode.jsのモジュールとして読み込まれます。
 * JavaScriptの構文を使って設定を記述できます。
 */

module.exports = {
  // ===== 基本設定 =====
  /**
   * 出力フォーマット
   * - json: JSON形式で出力
   * - csv: CSV形式で出力
   */
  format: 'json',
  
  // ===== リスト表示設定 =====
  // リスト表示時に取得する最大データセット数
  limit: '100',
  
  // リストモードで詳細情報を表示するかどうか
  detailed: false,
  
  // ===== API設定 =====
  // 言語設定（J: 日本語, E: 英語）
  language: 'J',
  
  // 解説情報取得フラグ（Y/N）
  explanationGetFlg: 'N',
  
  // 注釈情報取得フラグ（Y/N）
  annotationGetFlg: 'N',
  
  // セクションヘッダーフラグ（0/1）
  sectionHeaderFlg: '1',
  
  // 特殊文字置換フラグ（0/1）
  replaceSpChars: '0',
  
  // ===== 統計データ =====
  /**
   * 取得する統計データID（複数指定可能）
   * 例: 00200502 - 社会・人口統計体系
   */
  statsData: [
    {
      // 統計表ID
      statsId: '0000020201',
      // カテゴリコード（例: A1101 - 総人口）
      cdCat01: 'A1101',
      // 調査年（例: 2022100000）
      cdTimeFrom: '2000100000'
    },
    {
      // 別の統計データを指定する例
      statsId: '0000020205',
      cdCat01: 'E2101',
      // 特定の調査年を指定しない場合は省略可能
    }
  ],

  // 後方互換性のために単一指定も残しておく（非推奨）
  statsId: '0000020201',
  cdCat01: 'A1101',
  cdTimeFrom: '2000100000',
};
